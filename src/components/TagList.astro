---
const { tags } = Astro.props;
---

<div class="tag-list">
  <div class="tags">
    {
      tags.map((tag: any) => <p class="tag blog-button"><a href={`/tags/${tag}`}>{tag}</a></p>)
    }
  </div>
</div>